export type TimelineItem = {
  period: string;
  title: string;
  organization: string;
  description: string;
  icon: "education" | "internship" | "work";
};

export type AboutCardData = {
  title: string;
  description: string;
  icon: string;
};